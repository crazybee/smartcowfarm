---
# Ansible Galaxy collections required by all playbooks.
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: azure.azcollection
    version: ">=2.4.0"
  - name: community.docker
    version: ">=3.10.0"
  - name: community.general
    version: ">=9.0.0"
